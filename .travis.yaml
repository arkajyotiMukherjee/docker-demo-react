language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t arkajyotiMukherjee/docker-demo-react -f Dockerfile.dev .

script:
  - docker run -e CI=true arkajyotiMukherjee/docker-demo-react npm run test
